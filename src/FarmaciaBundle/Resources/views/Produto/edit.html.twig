{% extends 'base.html.twig' %}

{% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}
    <h1>Editar produto</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Salvar" class="btn btn-success pull-right" />
    {{ form_end(edit_form) }}
    
    {{ form_start(delete_form) }}
        <input type="submit" value="Apagar" class="btn btn-danger pull-right" style="margin-right:2%;">
    {{ form_end(delete_form) }}
    
    <a href="{{ path('produto_index') }}" class="btn btn-primary">Voltar  para a lista</a>
    
{% endblock %}
